# Workspace Packages

- `engine`: deterministic interpreter and validation
- `level-schema`: shared schema and runtime validation
- `level-data`: starter curriculum content
- `editor`: internal authoring helpers
