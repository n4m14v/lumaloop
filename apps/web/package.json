{
  "name": "@lumaloop/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@lumaloop/engine": "workspace:*",
    "@lumaloop/level-data": "workspace:*",
    "@lumaloop/level-schema": "workspace:*",
    "@react-three/drei": "^10.7.6",
    "@react-three/fiber": "^9.4.0",
    "@tailwindcss/vite": "^4.1.13",
    "@types/three": "^0.180.0",
    "gsap": "^3.13.0",
    "lucide-react": "^0.577.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwindcss": "^4.1.13",
    "three": "^0.180.0",
    "zustand": "^5.0.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && vite build",
    "build:measure": "pnpm run build && node ./scripts/measure-build.mjs",
    "dev": "vite",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build:hub": "VITE_BASE_PATH=/lumaloop/ pnpm run build",
    "dev:hub": "VITE_BASE_PATH=/lumaloop/ pnpm run dev"
  }
}
